<div class="note" data-note-id={{_id}} data-creator={{creator}} data-frittertext={{content}}>
  <p><b>{{creator}}</b></p>
  <p>{{content}}</p>
  <a href="#" class="refritter-note">ReFritter</a>
</div>
